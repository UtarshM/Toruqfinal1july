{
  "name": "toque-insurance",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "admin-panel",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev:admin": "cd admin-panel && npm run dev",
    "dev:frontend": "cd frontend && npm run web",
    "dev": "concurrently \"npm run dev:admin\" \"npm run dev:frontend\"",
    "prisma:generate": "cd admin-panel && npx prisma generate",
    "prisma:push": "cd admin-panel && npx prisma db push",
    "build": "cd admin-panel && npx prisma generate && npm run build",
    "vercel-build": "cd admin-panel && npx prisma generate && npm run build",
    "ota:publish": "node scripts/publish-ota.js",
    "ota:publish:preview": "node scripts/publish-ota.js --channel preview",
    "ota:publish:prod": "node scripts/publish-ota.js --channel production",
    "ota:status": "cd frontend && npx eas update:list --branch preview --limit 3 --non-interactive",
    "ota:status:prod": "cd frontend && npx eas update:list --branch production --limit 3 --non-interactive"
  },
  "devDependencies": {
    "babel-preset-expo": "~54.0.11",
    "concurrently": "^10.0.3",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "dependencies": {
    "expo-updates": "~29.0.18",
    "pdf-lib": "^1.17.1"
  }
}
